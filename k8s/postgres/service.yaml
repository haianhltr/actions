apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: actions
  labels:
    app: postgres
    team: team6
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
