apiVersion: v1
kind: Service
metadata:
  name: ops-ui
  namespace: actions
  labels:
    app: ops-ui
    service: ops-ui
    team: team6
spec:
  type: NodePort
  selector:
    app: ops-ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 31080
      protocol: TCP
